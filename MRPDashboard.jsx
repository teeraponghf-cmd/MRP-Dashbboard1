import React from "react";
import ReactDOM from "react-dom/client";
import MRPDashboard from "./MRPDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MRPDashboard />
  </React.StrictMode>
);
